export PORT=${PORT:=5173}

# Open the sidecar window when entering the project (direnv allow)
# Keeps running in background; the script itself waits until the dev server responds.
(npm run open:sidecar >/dev/null 2>&1 &)

# Optionally, load Node via asdf/nvm here if you use them
# use asdf
# layout node

